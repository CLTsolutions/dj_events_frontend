// have to restart dev server whenever create or add to this file
{
  "compilerOptions": {
    "baseUrl": ".",
    // paths in an object where we create aliases
    // each folder want to create
    // /* is anything in that folder
    "paths": {
      "@/components/*": ["components/*"],
      "@/styles/*": ["styles/*"],
      "@/config/*": ["config/*"]
    }
  }
}
